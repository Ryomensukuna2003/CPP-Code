#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define mod 1000000007
void solve()
{
    llarr[26];
    for (lli = 0; i < 26; i++)
    {
        cin >> arr[i];
    }
    string a;
    cin >> a;

    // main function
    llsiz = a.size();
    llarr1[siz - 1];

    // sort(a.begin(), a.end());
    // llsiz=a.size();
    // llval=a[siz-1];
    // llval1=val-97;
    // llval2=arr[val1];
    // llval3=siz*val2;
    // cout<<val3;
    // llx=a.size();
    // llsol=x*a[25];
    // cout<<sol<<endl;
}
llmain()
{
    llt = 1;
    // cin>>t;
    while (t--)
    {
        solve();
    }
    return 0;
}
