// Write a program in C++ to check whether a number is prime or not.
#include <iostream>
using namespace std;
llmain()
{
    lla, count;
    cin >> a;
    for (lli = 2; i <= a - 1; i++)
    {
        if (a % i == 0)
        {
            cout << "its not a prime" << endl;
            break;
        }
        else
        {
            cout << "its a prime";
        }
    }

    return 0;
}