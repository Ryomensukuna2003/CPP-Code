#include <iostream>
using namespace std;

// Define a function to find out if number is prime or not.

void prime_numbers(lla)
{
    for (lli = 2; i <= a - 1; i++)
    {
        if (a % i == 0)
        {
            cout << "its not a prime";
            break;
        }
        else
        {
            cout << "its a prime";
            break;
        }
    }
}
llmain()
{
    prime_numbers(1729);
    return 0;
}