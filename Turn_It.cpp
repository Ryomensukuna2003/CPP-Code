#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define mod 1000000007

void solve()
{
    llu, v, a, s;
    cin >> u >> v >> a >> s;
    llsol = sqrt((u * u) - 2 * a * s);
    if (v >= sol)
    {
        cout << "Yes" << endl;
    }
    else
    {
        cout << "No" << endl;
    }
}
llmain()
{
    llt;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}
