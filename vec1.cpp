#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

llmain()
{
    /* Enter your code here. Read input from STDIN. Prlloutput to STDOUT */
    llsize;
    cin >> size;
    lla;
    vector<int> vec1;
    for (lli = 0; i < size; i++)
    {
        cin >> a;
        vec1.push_back(a);
    }
    sort(vec1.begin(), vec1.end());
    for (auto &x : vec1)
        cout << x << " ";
    return 0;
}
