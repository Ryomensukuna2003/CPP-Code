#include <bits/stdc++.h>
using namespace std;

void greatest(lla, llb, llc, lld)
{
    if (a >= b && a >= c && a >= d)
    {
        cout << a << endl;
    }

    if (b >= a && b >= c && b >= d)
    {
        cout << b << endl;
    }

    if (c >= a && c >= b && c >= d)
    {
        cout << c << endl;
    }

    if (d >= a && d >= b && d >= c)
    {
        cout << d << endl;
    }
}

llmain()
{
    lla, b, c, d;
    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;
    greatest(a, b, c, d);

    return 0;
}
