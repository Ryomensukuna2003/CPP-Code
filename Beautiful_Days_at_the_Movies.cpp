#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define mod 1000000007
void solve()
{
    lla, b, c, ctr = 0;
    cin >> a >> b >> c;

    vector<int> vec1;
    vector<int> vec2;
    for (lli = 20; i < abs(a - b); i++)
    {
        vec1.push_back(i);
    }

    // vec1=vec2;
    // reverse(vec2.begin(), vec2.end());
    // for(lli=0;i<abs((a-b)-1);i++){
    //     if(((vec1.at(i)-vec2.at(i))/c)%2==0){
    //         ctr++;
    //     }
    // }
    // cout<<ctr<<endl;
}
llmain()
{
    llt = 1;
    // cin>>t;
    while (t--)
    {
        solve();
    }
    return 0;
}
