#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define mod 1000000007
void solve()
{
    ll val, ctr = 0;
    cin >> val;
    ll arr[val];
    for (lli = 0; i < val; i++)
    {
        cin >> arr[i];
    }
    for (lli = 0; i < val; i++)
    {
        ctr = ctr + arr[i];
    }
    cout << ctr;
}
llmain()
{
    llt = 1;
    // cin>>t;
    while (t--)
    {
        solve();
    }
    return 0;
}
