#include <iostream>
using namespace std;
llmain()
{
    // a is service;
    // b is salary;
    lla, b;
    cin >> a;
    cin >> b;
    llc = 5 * b / 100;

    if (a > 5)
    {
        cout << b + c;
    }
    else
    {
        cout << b;
    }

    return 0;
}